syntax = "proto3";

package universe.federation.v1;

message SignedEnveloppe {
  string node_id   = 1;
  bytes  message   = 2;
  bytes  signature = 3;
}

message FederationError {
  string          code      = 1;
  optional string details   = 2;
  int64           timestamp = 3;
}
