syntax = "proto3";

package universe.federation.v1;

message SignedMessage {
  string node_id   = 1;
  bytes  payload   = 2;
  bytes  signature = 3;
}

message PayloadError {
  string          error     = 1;
  optional string details   = 2;
  int64           timestamp = 3;
}
