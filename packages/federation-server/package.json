{
  "name": "@universe/federation-server",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "check": "biome check",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:migrate": "DOTENV_CONFIG_PATH=tests/.test.env prisma migrate dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/pg": "^8.20.0",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "dependencies": {
    "@prisma/adapter-pg": "catalog:",
    "@universe/game-protocol": "workspace:*",
    "@universe/server-shared": "workspace:*",
    "commander": "^14.0.3",
    "dotenv": "catalog:",
    "fastify": "^5.8.5",
    "pg": "^8.21.0",
    "zod": "catalog:"
  }
}
