{
  "name": "@universe/server-federation-plugin",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "type": "module",
  "exports": "./src/index.ts",
  "scripts": {
    "check": "biome check",
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:migrate": "DOTENV_CONFIG_PATH=tests/.test.env prisma migrate dev",
    "clean": "rm -rf dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/pg": "^8.20.0",
    "tsx": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "dependencies": {
    "@universe/game-protocol": "workspace:*",
    "@universe/server-shared": "workspace:*",
    "fastify": "^5.8.5",
    "pg": "^8.21.0",
    "zod": "catalog:"
  }
}
